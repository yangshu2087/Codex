interface:
  display_name: "Task Analyzer"
  short_description: "Task analysis and skill selection"
  default_prompt: "Use $task-analyzer to analyze this task and select skills."

policy:
  allow_implicit_invocation: true
