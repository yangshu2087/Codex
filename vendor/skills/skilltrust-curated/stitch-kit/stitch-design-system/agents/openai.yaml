interface:
  display_name: "Stitch Design System"
  short_description: "Use when the user explicitly mentions Google Stitch and wants design tokens, CSS custom properties, Tailwind theme guidance, or code-level..."
  default_prompt: "Use $stitch-design-system only for an explicit on-demand review."

policy:
  allow_implicit_invocation: false
