interface:
  display_name: "Stitch React Components"
  short_description: "Use when the user explicitly mentions Google Stitch and asks to convert Stitch designs into Vite, CRA, or generic React components."
  default_prompt: "Use $stitch-react-components only for an explicit on-demand review."

policy:
  allow_implicit_invocation: false
